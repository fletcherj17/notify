<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/main.css">
    <link rel="shortcut icon" href="/images/notify-icon.png">
    <link href="https://fonts.googleapis.com/css2?family=Lexend+Tera&display=swap" rel="stylesheet">
    <title>Edit <%= song.title%></title>
</head>
<body>
    <div>
        <nav>
            <ul>
                <li><h1>N</h1><img src="/images/notify-logo.png" alt="notify logo" id="logo"><h1>Tify</h1></li>
                <li><a href="/index">Home</a></li>
                <li><a href="/songs">Songs</a></li>
                <li><a href="/logout">Log out</a></li>
            </ul>
        </nav>
    <main>
        <h2>Edit "<%= song.title%>"</h2>
        <form action="/songs/<%= song._id %>?_method=PUT" method="POST">
        Title: <input type="text" name="title" value="<%= song.title %>" required>
        <p>Add to playlist? <select name="playlistId">
            <option value="false">Select Playlist</option>
            <% for( let i = 0; i < playlists.length; i++ ) { %>
            <option value="<%= playlists[i]._id %>"><%= playlists[i].title %></option>
            <% } %>
        </select></p>
        <input class="button" type="submit" value="Update Song">
        </form>
    </main>
    </div>
</body>
</html>

